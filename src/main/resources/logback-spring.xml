<configuration>

    <appender name="SYSTEM_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/system/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/system/app.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="LOGIN_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/login/login-related.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/login/login-related.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>90</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [AUTH] %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/error/error.log</file>
        
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/error/error.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>10</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [ERROR] %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ========================================== -->
    <!-- Log 路由規則 -->
    <!-- ========================================== -->

    <logger name="com.tavinki.taskiu.modules.auth" level="INFO" additivity="false">
        <appender-ref ref="LOGIN_FILE" />
    </logger>

    <logger name ="com.tavinki.taskiu.modules.email" level="INFO" additivity="false">
        <appender-ref ref="LOGIN_FILE" />
    </logger>

    <!-- TRACE < DEBUG < INFO < WARN < ERROR  -->
    <!-- 設定為 INFO，代表 INFO, WARN, ERROR 都會流進來 -->
    <root level="INFO">
        <!-- SYSTEM_FILE 會收到 INFO, WARN, ERROR -->
        <appender-ref ref="SYSTEM_FILE" />
        
        <!-- ERROR_FILE 也會收到 INFO, WARN, ERROR，但因為它自己內部有 Filter，所以只會寫入 ERROR -->
        <appender-ref ref="ERROR_FILE" />
    </root>

</configuration>
